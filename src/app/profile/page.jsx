
export const metadata = {
  title: "My Profile | IdeaVault",
  description: "Manage your profile, update your name and profile picture on IdeaVault.",
};

import ProfileClient from '@/components/MainRouteClient/ProfileClient';

export default function ProfilePage() {
  return <ProfileClient />;
}